#!/bin/sh
if [ -z "$D" -a -f "/etc/init.d/quectel_slic_daemon" ]; then
	/etc/init.d/quectel_slic_daemon stop
fi
if type update-rc.d >/dev/null 2>/dev/null; then
	if [ -n "$D" ]; then
		OPT="-f -r $D"
	else
		OPT="-f"
	fi
	update-rc.d $OPT quectel_slic_daemon remove
fi
